import styled from "styled-components";

export default styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  background-color: var(--cr-fondo-visible);
  color: white;
  padding: 0 var(--px-padding-base);
  border-top: 1px solid var(--cr-linea);
`;
