import styled from "styled-components";

export default styled.div`
  min-height: var(--px-alto-header);
  padding: 0 var(--px-padding-base);
  background-color: white;
  display: grid;
  align-items: center;
`;
